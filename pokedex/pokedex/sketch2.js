function setup() {
  var myCanvas = createCanvas(255, 175);
  myCanvas.parent('stats');
}

function draw() {
  // These commands are applied to the graphics canvas as normal.
  background(255, 255, 255);
}
